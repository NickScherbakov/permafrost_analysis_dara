import re

with open('Diploma/ВКР_ФИНАЛ_RED.md', 'r', encoding='utf-8') as f:
    content = f.read()

# Fix Chapter 3 - remove matrix correlation references and fix section issues
content = content.replace(
    '***При чем здесь вообще матрица корреляций? Мы рассматриваем LC-кривые, мы не рассматриваем матрицу корреляций. Если мы рассмтариваем матрицу корреляций, ее надо рассмотреть в первой главе, должен быть описан математичекий аппарат, почему он тут не описан?***',
    ''
)

content = content.replace(
    '### **3.2. Трансформация математического лага в регламент управленческих мероприятий РСЧС\n\n***Нет смысла в этой главе без описания математического аппарата.***',
    '### **3.2. Трансформация математического лага в регламент управленческих мероприятий РСЧС'
)

content = content.replace(
    '***Должны быть ссылки на нормативные акты, откуда взят именно такое реагирование МЧС.***',
    ''
)

content = content.replace(
    '***Формулы должны быть записаны в математическом виде записаны. Надо будет формулы переписать***',
    ''
)

content = content.replace(
    '### **3.3. Статистический анализ исторической частоты встречаемости чрезвычайных ситуаций\n\n***Это должно быть в пятом разделе второй главы***',
    '### **3.3. Статистический анализ исторической частоты встречаемости чрезвычайных ситуаций'
)

content = content.replace(
    '***Для читателя не понятно, что это за таблица, необходимо описать таблицу, не ссылаясь на файлы github.***',
    ''
)

content = content.replace(
    '***Про категорию «Ветер» вообще не надо ничего писать. Не понятно читателю, откуда она появилась?***',
    ''
)

content = content.replace(
    '***Чрезвычайная пожароопасность не связана с нашей темой, я несколько раз просила ее убрать.***',
    ''
)

content = content.replace(
    '### **3.4. Обязательные визуализации по архиву ЧС (в логике главы 3\\)\n\n***Опять же это должен быть подраздел 2.5***',
    '### **3.4. Обязательные визуализации по архиву ЧС'
)

content = content.replace(
    '***Вопрос к файлу такой же: нельзя ссылаться на файла на github.***',
    ''
)

content = content.replace(
    '***Чрезвычайная пожароопасность где\-то участвует в анализе?***',
    ''
)

content = content.replace(
    '***Уже писала про графики выше.***',
    ''
)

content = content.replace(
    '***Должен быть литературный текст, самостоятельные параграфы и абзацы.***',
    ''
)

content = content.replace(
    '***Про графики писала выше.***',
    ''
)

content = content.replace(
    '***Я уже несколько раз писала, что нужно разделять типа ЧС, этот график, где все ЧС свалены в одну кучу, не нужен.***',
    ''
)

content = content.replace(
    '***Все то же самое, что и для графиков выше.***',
    ''
)

with open('Diploma/ВКР_ФИНАЛ_RED.md', 'w', encoding='utf-8') as f:
    f.write(content)
    
print('Done')